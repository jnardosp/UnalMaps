<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UnalMaps" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- To force only light theme -->
        <item name="android:forceDarkAllowed">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/darkPrimaryColor</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/accentColor</item>
        <item name="colorSecondaryVariant">@color/accentColor</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        
    </style>

    <style name="mainTextView">
        <item name="android:textColorLink">@color/primaryColor</item>
    </style>

    <style name="tabs">
        <item name="android:background">@color/lightPrimaryColor</item>
    </style>

    <style name="locateMeButton">
        <item name="android:backgroundTint">@color/accentColor</item>
    </style>

    <style name="configButton">
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="backgroundTint">@color/primaryColor</item>
    </style>

    <style name="Theme.UnalMaps.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.UnalMaps.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.UnalMaps.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>